Webpack Config "postcss-loader" with Immediate Config Demo
==========================================================

不需要`postcss.config.js`，直接在webpack中使用postcss-loader的地方设置plugins

```
npm install
npm run demo
```
